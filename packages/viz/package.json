{
  "name": "@testchimp/semantic-graph-viz",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp assets/ChimpLogoRed.svg dist/"
  }
}
